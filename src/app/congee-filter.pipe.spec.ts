import { CongeeFilterPipe } from './congee-filter.pipe';

describe('CongeeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CongeeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
