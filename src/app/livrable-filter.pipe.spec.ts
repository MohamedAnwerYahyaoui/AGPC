import { LivrableFilterPipe } from './livrable-filter.pipe';

describe('LivrableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LivrableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
